#include "XRPointerDevice.hpp"
#ifdef HAVE_OPENXR

// CXRPointerDevice is header-only (an IPointer subclass exactly like CTestMouse); this TU exists
// so the class has a translation unit and to keep the file pair the roadmap (WP7) lists.

#endif // HAVE_OPENXR
